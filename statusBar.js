/*export default class StatusBar {
  constructor(x, y, maxValue, name) {
    this.x = x;
    this.y = y;
    this.maxValue = maxValue;
    this.name = name;
  }
}

export default class Happiness extends StatusBar {
  constructor(x, y, maxValue, name) {
    super(x, y, maxValue, name);
  }
  render() {
    fill(0, 255, 0);
    rect(this.x, this.y, 50, 50);
  }*/
